# Backend API URL for development
# When running frontend in development mode with Vite
VITE_API_URL=http://localhost:8080/api

# For production (Docker), this is handled by Nginx proxy
# VITE_API_URL=/api
